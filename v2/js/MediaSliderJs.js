var currentIndex = 0;
var mediaCount = 0;

function init() {
    currentIndex = 0;
    mediaCount = 0;
}

function setMediaCount(count) {
    mediaCount = count;
}

function getMediaCount() {
    return mediaCount;
}